# Configures the build system, enabling compilations and linking of the bootloader stages

# Configure build process
# Enables NASM for assembling stage 1 
# Defines how to build stage 1 and 2 bootloader binaries
# Combines binaries into bootable image